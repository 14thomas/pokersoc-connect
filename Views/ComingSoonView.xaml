<UserControl x:Class="pokersoc_connect.Views.ComingSoonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="600" MinHeight="400">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Grid Grid.Row="0" Margin="16,16,16,0">
      <TextBlock x:Name="TitleText" Text="Coming Soon" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
      <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Center"
              Width="30" Height="30" Click="Close_Click" FontSize="16"/>
    </Grid>

    <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="16">
      <TextBlock x:Name="MessageText"
                 Text="This feature is not available yet."
                 FontSize="20"
                 HorizontalAlignment="Center"
                 TextAlignment="Center"
                 TextWrapping="Wrap"
                 Margin="0,0,0,24"/>
      <Button Content="Back to Main" Padding="20,12" FontSize="16" Click="Close_Click" HorizontalAlignment="Center"/>
    </StackPanel>
  </Grid>
</UserControl>
