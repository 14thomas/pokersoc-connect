<UserControl x:Class="pokersoc_connect.Views.TournamentInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="600" MinHeight="400">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <Grid Grid.Row="0" Margin="0,0,0,24">
      <TextBlock Text="Tournament Info" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
      <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Center"
              Width="30" Height="30" Click="Close_Click" FontSize="16"/>
    </Grid>

    <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
      <Grid HorizontalAlignment="Center">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="240"/>
          <ColumnDefinition Width="40"/>
          <ColumnDefinition Width="240"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" BorderBrush="#2E6DA4" BorderThickness="2" Background="#E8F4FD"
                Padding="24" CornerRadius="4" VerticalAlignment="Top">
          <StackPanel HorizontalAlignment="Center">
            <TextBlock Text="Total Buy-ins" FontSize="16" FontWeight="Bold"
                       HorizontalAlignment="Center" Opacity="0.8"/>
            <TextBlock x:Name="TotalBuyInsText" Text="0" FontSize="48" FontWeight="Bold"
                       HorizontalAlignment="Center" Foreground="#2E6DA4" Margin="0,8,0,0"/>
            <StackPanel x:Name="BuyInBreakdownPanel" Margin="0,12,0,0" HorizontalAlignment="Stretch"/>
          </StackPanel>
        </Border>

        <Border Grid.Column="2" BorderBrush="#B8860B" BorderThickness="2" Background="#FFF8E8"
                Padding="24" CornerRadius="4" VerticalAlignment="Top">
          <StackPanel HorizontalAlignment="Center">
            <TextBlock Text="Total Rebuys" FontSize="16" FontWeight="Bold"
                       HorizontalAlignment="Center" Opacity="0.8"/>
            <TextBlock x:Name="TotalRebuysText" Text="0" FontSize="48" FontWeight="Bold"
                       HorizontalAlignment="Center" Foreground="#B8860B" Margin="0,8,0,0"/>
            <StackPanel x:Name="RebuyBreakdownPanel" Margin="0,12,0,0" HorizontalAlignment="Stretch"/>
          </StackPanel>
        </Border>
      </Grid>
    </StackPanel>

    <Button Grid.Row="2" Content="Back to Main" Padding="20,12" FontSize="16"
            Click="Close_Click" HorizontalAlignment="Center" Margin="0,16,0,0"/>
  </Grid>
</UserControl>
