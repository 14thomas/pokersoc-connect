<UserControl x:Class="pokersoc_connect.Views.PlayerManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Header -->
    <Grid Grid.Row="0" Margin="0,0,0,20">
      <TextBlock Text="Player Database" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
      <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Center" 
              Width="30" Height="30" Click="Close_Click" FontSize="16"/>
    </Grid>

    <!-- Player List -->
    <DataGrid x:Name="PlayersGrid"
              Grid.Row="1"
              AutoGenerateColumns="False"
              IsReadOnly="True"
              Margin="0,0,0,20">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Player ID" Binding="{Binding player_id}" Width="120"/>
        <DataGridTextColumn Header="Name" Binding="{Binding display_name}" Width="150"/>
        <DataGridTextColumn Header="Email" Binding="{Binding email}" Width="150"/>
        <DataGridTextColumn Header="Student Number" Binding="{Binding student_number}" Width="120"/>
        <DataGridTextColumn Header="Degree" Binding="{Binding degree}" Width="120"/>
        <DataGridTextColumn Header="Study Year" Binding="{Binding study_year}" Width="100"/>
        <DataGridTextColumn Header="Arc Member" Binding="{Binding arc_member}" Width="100"/>
        <DataGridTextColumn Header="Created" Binding="{Binding created_at}" Width="150"/>
      </DataGrid.Columns>
    </DataGrid>

    <!-- Action Buttons -->
    <Grid Grid.Row="2">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>

      <Button Grid.Column="0" Content="Import Players" 
              Click="Import_Click" 
              Padding="15,10" 
              Margin="0,0,5,0"
              FontSize="14" 
              FontWeight="Bold"/>

      <Button Grid.Column="1" Content="Export Players" 
              Click="Export_Click" 
              Padding="15,10" 
              Margin="5,0,5,0"
              FontSize="14" 
              FontWeight="Bold"/>

      <Button Grid.Column="2" Content="Refresh" 
              Click="Refresh_Click" 
              Padding="15,10" 
              Margin="5,0,5,0"
              FontSize="14" 
              FontWeight="Bold"/>

      <Button Grid.Column="3" Content="Close" 
              Click="Close_Click" 
              Padding="15,10" 
              Margin="5,0,0,0"
              FontSize="14" 
              FontWeight="Bold"/>
    </Grid>
  </Grid>
</UserControl>
